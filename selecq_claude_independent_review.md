# SelecQ 독립 평가 (Claude) — GPT 피드백 확인 전 작성

> 작성일: 2026-07-23. GPT 피드백을 보기 전에 작성한 독립 평가.
> 방법: 배포 사이트(gichul-select.pages.dev)를 실제 브라우저로 조작(필터 선택 → 풀이 → 채점 → 오답/기록 확인, 모바일 뷰포트 검사) + 소스 코드(index.html, app.js 1,457줄, styles.css) 검토.

---

## 총평

MVP로서 완성도가 높다. 핵심 루프(필터 → 풀기 → 채점 → 오답 → 재도전)가 실제로 끊김 없이 동작하고, 펜 필기·타이머·백업/복원·PDF 내보내기까지 학습 도구로서의 디테일이 갖춰져 있다. 반면 **검색엔진/외부 노출(SEO)과 접근성은 구조적으로 취약**하고, **저작권 문구와 실제 제공물의 불일치**가 가장 큰 리스크다.

## 실제 동작 확인 결과 (GPT가 못 본 영역)

- 필터 선택 → "110문제 풀기" 버튼 생성 → 풀이 화면 진입: 정상
- 풀이 화면: 문제 크롭 이미지 정상 로드(753px, alt 텍스트 있음), 펜/지우개 canvas, 확대/축소, 집중 모드, 원본 PDF 링크, 출처 표기, 해설 구글 검색 링크, 이전/다음/목록(1/110) 모두 동작
- 채점: 오답 처리 정상("❌ 오답 정답 ⑤") → 오답 목록에 자동 반영 → 기록 페이지에 정답률·약한 유형·백업/복원 표시. **핵심 데이터 루프가 실제로 완결됨**
- 나만의 모의고사: 직접 선택/조건 랜덤 탭, 타이머(없음/20/50/100/직접), 세트 담기 UI 정상
- 회차별 실전: 학년도(2022~2027)·시험·선택과목 3단 선택 → 30문항·100분 구성 안내 명확
- 모바일(375px): 가로 스크롤 없음, 답 선택 버튼 46×46px(터치 타깃 기준 44px 충족). 단, 상단 nav가 16px 오버플로(302>286) — 마지막 메뉴가 잘리거나 스크롤됨
- 콘솔 에러: 없음

## 잘된 점

1. **핵심 루프 완결성** — 풀이 기록이 오답·통계·풀이상태 필터("안 푼 문제만")까지 일관되게 흐른다.
2. **코드 품질** — IIFE 스코프, 방어적 JSON 파싱, 필터 컨텍스트 분리(browse/custom 독립 유지) 같은 설계 판단이 좋고 주석이 "왜"를 설명한다. 1,457줄 단일 파일은 이 규모에선 오히려 적절.
3. **학습 도구 디테일** — 문항별 체류시간 기록(beforeunload flush까지 처리), 난이도별 기준시간으로 랜덤 세트 타이머 자동 산정, 유형 오류 신고 링크.
4. **데이터 리스크 대비** — localStorage 한계를 백업/복원 파일로 보완하고, 그 한계를 UI에 정직하게 고지.
5. **출처·저작권 표기 의식** — 문항마다 출처 문구와 원본 PDF 링크 제공.

## 이슈 (심각도순)

### A. 저작권 문구 불일치 — 리스크 최상
푸터/안내문은 "문항 메타데이터와 원본 링크만 제공"이라고 하지만, 실제로는 **문항 크롭 이미지 736장을 직접 서빙**한다(q/*.png). KICE/EBS 허락 대기 중인 걸로 아는데, 허락 전까지는 안내문이 사실과 다르다는 점 자체가 불리하게 작용할 수 있다. 문구를 실제 제공물에 맞게 고치거나, 허락 전 노출 범위를 조정하는 판단 필요.

### B. SEO/외부 노출 — 구조적 취약
- CSR + 해시 라우팅: 내부 화면(#/find 등)이 검색엔진에 사실상 존재하지 않음
- `index.html`에 **meta description 없음, Open Graph/Twitter 카드 없음, canonical 없음, favicon(.ico) 없음, robots.txt/sitemap 없음**
- 카카오톡/SNS 공유 시 미리보기가 빈약하게 나옴 → 초기 유입에 직접 타격
- 최소 조치(메타태그·OG 이미지)만으로도 체감 개선 가능. 근본 해결은 프리렌더링/경로 라우팅이지만 공수 대비 우선순위는 낮음

### C. 접근성 — 필터 칩이 키보드로 조작 불가
드롭다운 안의 선택지가 `<span class="chip">`(role 없음, tabindex 없음)라서 **키보드·스크린리더로는 필터를 아예 걸 수 없다.** 상단 필터 버튼과 답 선택은 `<button>`이라 문제없음. `<button class="chip">`으로 바꾸는 게 정석이고 비용도 낮다.

### D. 성능 — data.js 395KB 동기 로드
첫 진입 시 395KB(질문 메타 전체) + 폰트 + 앱 코드를 다 받아야 첫 화면이 뜬다. 모바일 3G/LTE에서 첫 로딩 수 초. gzip이면 실제 전송량은 줄겠지만, 장기적으로 과목별 분할 로드나 지연 로드 고려 대상.

### E. 소소한 것들
- 모바일 nav 16px 오버플로 (위 확인 결과 참조)
- `innerHTML` 템플릿에 이스케이프 함수가 없음 — 현재 데이터가 자체 생성이라 실위험은 낮지만, 백업 **복원 파일**을 통해 임의 문자열이 기록에 들어올 수 있는 경로가 있다면 점검 가치 있음
- 홈과 #/find가 기능적으로 거의 동일(같은 필터 UI) — 신규 방문자에게 둘의 차이가 불명확할 수 있음

## GPT 피드백과 교차 검증 시 관전 포인트

- GPT는 렌더링된 화면을 못 봤으므로, **UX 흐름·상호작용 관련 지적은 코드 추론에 근거** → 실제 동작 확인 결과(위)와 대조할 것
- 둘 다 지적한 항목 = 우선순위 상향, 한쪽만 지적한 항목 = 근거 확인 후 판단
