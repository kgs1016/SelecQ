# SelecQ 해설 생성 규칙 (v1 · 2026-07-27)

이 문서는 SelecQ 자체 해설을 생성할 때 **모델(사람·AI·서브에이전트 무관)이 반드시 따르는 기준**이다.
목표: *실제 고등학생이 그 과목에서 배운 방법으로* 푼, 깔끔하고 정확한 해설.
생성 결과는 `webapp/solutions.js`에 넣고 앱에서 KaTeX로 렌더된다.

---

## 0. 3대 원칙

1. **정확성** — 최종 답은 `data.js`의 `answer`와 반드시 일치. 불일치 시 폐기·재생성. 사람 검수 전제.
2. **커리큘럼 충실** — 그 문제가 속한 **과목 범위 안의 방법만** 사용(§2). 답만 맞으면 되는 게 아니다.
3. **한국 해설 문체** — 학원·EBS 해설처럼 자연스럽게. AI 티(§3) 금지.

---

## 1. 생성 워크플로우 (참조 기반)

문항 하나당:

1. `data.js`에서 메타 확보: `answer`(정답 앵커), `subject`, `unit`, `type`, `points`, `difficulty`, `img`.
2. 문제 이미지(`webapp/q/<key>.png`)로 문제 파악.
3. **공식 해설의 *방법*을 참조** — 우선순위 **종로학원 · 이투스**, 없는 회차는 EBS.
   (공식 해설은 그 과목 커리큘럼 안에서만 풀므로, 방법을 따르면 §2 위반이 원천 차단된다.)
   - 참조 자료는 `평가원해설/` 폴더(로컬 전용). PDF 텍스트는 깨지므로 해당 문제 페이지를 이미지로 렌더해 확인
     (poppler 없음 → `pymupdf`로 페이지→PNG). 시험별 **문제→페이지 인덱스**를 1회 구축해두면 즉시 참조.
4. **방법은 참조, 표현·그래프는 100% 자체 제작**(§7 저작권).
5. 답을 앵커로 두고 **간결하게** 서술. 재유도(맨땅 풀이)로 토큰 낭비 금지.
6. 필요 시 그래프 명세 → `matplotlib` 렌더(§5).
7. 검증(§6) 통과 후 `solutions.js`에 저장(§4 포맷).

> 난이도 무관 — **중·하 문항도** 위 기준(특히 §2·§3)을 똑같이 적용한다.

---

## 2. 과목별 허용/금지 스킬 화이트리스트  ★핵심★

`data.js`의 `subject`/`unit`로 자동 분기. **문제 범위 밖 기법은 답이 맞아도 금지.**
(2015 개정 교육과정 기준 — 2022~2027학년도 수능)

### 공통 — 수학Ⅱ  (`unit`이 `su2_*`)
- **허용**: 함수의 극한·연속, 다항함수의 미분법((xⁿ)′, 상수배·합·차·**곱의 미분법**),
  접선, 증가·감소, 극대·극소, 그래프 개형, 방정식·부등식 활용, 다항함수의 적분법(부정·정적분),
  정적분의 활용(넓이, 속도와 거리).
- **금지(→ 미적분 과목 내용)**: 몫의 미분법, 합성함수 미분법, 매개변수·음함수·역함수 미분,
  **초월함수(eˣ, ln x, sin x …) 미분·적분**, 치환적분, 부분적분, 수열의 극한/급수, 로피탈 정리.

### 공통 — 수학Ⅰ  (`unit`이 `su1_*`)
- **허용**: 지수·로그(지수법칙·로그성질·그래프·방정식·부등식), 삼각함수(정의·그래프·사인/코사인법칙·삼각방정식),
  수열(등차·등비·Σ·수학적 귀납법).
- **금지**: **삼각함수의 미분·적분**((sin x)′ 등), 수열의 극한, 그 밖 미적분 과목 기법.

### 선택 — 미적분  (`subject` = `calculus`)
- **허용**: 수열의 극한·급수, 초월함수 미분·적분, 여러 미분법(몫·합성·매개·음함수·역함수),
  치환적분·부분적분, 정적분의 활용(넓이·부피·길이).

### 선택 — 확률과 통계  (`subject` = `prob_stat`)
- **허용**: 순열·조합·이항정리, 확률(조건부·독립·덧셈/곱셈정리), 확률분포(이항·정규, **주어진 표준정규분포표 사용**), 통계적 추정.
- **금지**: 미적분 기법.

### 선택 — 기하  (`subject` = `geometry`)
- **허용**: 이차곡선(포물선·타원·쌍곡선), 평면벡터(연산·내적), 공간도형·공간좌표.
- **금지**: 미적분 기법.

> 원칙 한 줄: **"이 문제를 실제로 보는 학생이 그 시험 범위에서 배운 도구만 쓴다."**

---

## 3. 문체 규칙 (AI 티 제거)

- **한국식 수학 표기**: "45와 16은 **서로소**"(O) / `gcd(45,16)=1`(X). "따라서/그러므로/∴"(O).
- **근사·노가다 금지**: 소수 대입·수치 근사로 뚫지 말 것. **정확한 값·대수적 방법**으로.
  (예: 0.333… 대신 1/3, 억지 대입 대신 인수분해·판별식.)
- **금지 말투(전형적 AI 티)**: "먼저 ~를 살펴보면", "따라서 우리는", "~임에 주목하자",
  "Let ~", "Note that", 불필요한 영어, 과잉 불릿, 매 단계 요약 반복.
- **간결·단정**: 학원 해설처럼 필요한 단계만. 사족·군더더기 없음.
- **난이도별 밀도**: 쉬움 1~2스텝, 중간 2~3스텝, 킬러 5~7스텝. 쉬운 문제를 장황하게 쓰지 말 것.
- 단위·기호는 문제와 일치. 답 표기: 객관식 "답 ④", 단답 "답 6".

---

## 4. 출력 포맷 (`solutions.js`)

```js
"<문항key>": { answer: <정답>, html: String.raw`
  ...본문...
` },
```

- `key`는 `data.js`와 동일(예: `2022_mock06/common/q22`). `answer`도 `data.js`와 일치.
- `String.raw` 필수 — `\frac`, `\(` 등 백슬래시 보존.
- **본문 HTML 구조**(스타일은 styles.css에 이미 있음):
  - 단계: `<div class="sol-step"><div class="sol-h"><span class="sol-num">N</span> 제목</div> ... </div>`
  - 강조 박스: `<div class="sol-box">...</div>`
  - 표: `<table class="sol-table">...</table>` (판정 셀은 `class="sol-ok"` / `sol-no`)
  - 그래프: `<figure class="sol-fig"><img src="sol/<key>.png" alt="..."><figcaption>...</figcaption></figure>`
  - 최종답: `<div class="sol-final">... \(\mathbf{답}\)</div>`

---

## 5. 수식·그래프 규칙

### KaTeX 표기
- 인라인 `\( ... \)`, 블록 `$$ ... $$`. (auto-render 델리미터가 이 둘)
- 분수 `\dfrac`/`\tfrac`, 적분 `\int_{a}^{b}`, 집합 `\cup \cap \in \notin`, 극한 `\lim_{x\to a}`.
- 단독 `$` 금지(델리미터 오인). 백슬래시 표기는 §4의 `String.raw` 안에서.
- **부등호는 반드시 `&lt;` `&gt;`로** — 수식 안 `<`/`>`를 그대로 쓰면 HTML 파서가 `<t`를 태그 시작으로 오인해
  뒤 수식이 통째로 깨진다. (`\(0&lt;t&lt;1\)` O / `\(0<t<1\)` X. `<0`처럼 숫자 앞은 태그가 안 되어 우연히 안전하지만, 일괄 `&lt;`/`&gt;` 권장.)

### 그래프 (모델은 *명세*만, matplotlib이 렌더)
- 그래프는 **꼭 필요한 문항만**(그래프형·넓이·개형·기하). 계산 문제엔 넣지 말 것.
- 모델은 **함수식·표시할 점·좌표범위·강조영역**만 명세 → 렌더 스크립트가 그림.
- 저장 경로 `webapp/sol/<key>.png`. **우리 자체 제작물**(공식 해설 그림 복제 금지).
- 스타일 통일: 곡선 `#2563eb`, 직선/강조 `#e11d48`·`#059669`, 채움 반투명, 격자 `alpha 0.25`,
  한글 폰트 `Malgun Gothic`, `mathtext.fontset='cm'`, `axes.unicode_minus=False`, dpi≈150.

---

## 6. 검증 게이트 (배포 전 자동)

1. **정답 대조**: 해설의 최종 답 == `data.js`의 `answer`. 불일치 → 폐기·재생성.
2. **금지기법 스캐너**: `subject`/`unit`에 따라 §2 금지 키워드가 본문에 있으면 **사람 검수 플래그**.
   - 수학Ⅱ 문항 스캔 예: `치환`, `부분적분`, `합성함수`, `몫의 미분`, `\ln`, `e^{`, `\lim_{n`, `(sin`, `\sin'`, 로피탈.
   - 수학Ⅰ 문항 스캔 예: 삼각함수 미분/적분 표현, `\lim`.
3. **문체 스캐너**: `gcd(`, "우리는", "살펴보면", "Note that", "Let " 등 → 플래그.
4. **사람 스팟체크**: 표본 검수(전수 재확인은 토큰 낭비).

---

## 7. 저작권 가드레일 (엄수)

- 공식 해설(EBS·종로·이투스)의 **아이디어/방법은 참조 가능**(방법 자체는 저작권 대상 아님),
  그러나 **문장·그림 표현은 저작권** → 반드시 우리 말·우리 그래프로 재작성.
- 평가원해설/ PDF는 **로컬 참조 전용, `webapp/`에 절대 미포함·미배포**.
- 문제 이미지 저작권은 KICE. 해설 하단에 "풀이·그래프는 SelecQ 자체 제작 / 문제 저작권 KICE" 표기.

---

## 8. 토큰 절약 방침

- **필요한 것부터(lazy)**: 전 문항 선생성 금지. 킬러·자주 보는 문항 우선, 나머지는 수요 기반.
- **참조 기반**으로 재유도 최소화(추론 토큰↓ + §2 자동 보장).
- **재검증 최소화**: 정답 대조·스캐너(공짜)로 거르고 사람은 표본만.
- **페이지 인덱스 1회** 구축으로 참조 찾기 수작업 제거.
- 모델 티어링(싼 모델로 대량)은 **§2 화이트리스트 주입 + §6 스캐너 통과 조건에서만**. 품질 우선 시 신중히.

---

## 부록 A. 좋은 예 vs 나쁜 예

| 구분 | 나쁜 예 (AI 티/범위 위반) | 좋은 예 |
|---|---|---|
| 서로소 | `gcd(45,16)=1` | "45와 16은 서로소이므로" |
| 수2 미분 | 몫의 미분법으로 f′ 계산 | 곱의 미분법·다항함수 미분으로 |
| 극한값 | 0.4999…≈0.5로 근사 | 인수분해 후 약분해 정확히 1/2 |
| 말투 | "먼저 조건을 살펴보면 우리는…" | "조건 (가)에서 …" |
| 삼각(수1) | (sin x)′=cos x 사용 | 삼각함수 값·항등식·사인법칙으로 |
